/**
 * @fileoverview The Footer Controller.
 */



/**
 * Controller for the Footer Directive.
 * @constructor
 */
function FooterController() {
  // Initialize the controller.
  this.init();
}


/** Export the module. */
module.exports = FooterController;


/**
 * List of dependencies required for this controller.
 * Necessary for Strict DI for safe dependency injection.
 * @type {!Array.<string>}
 */
FooterController.$inject = [];


/**
 * Initializes the controller properties.
 */
FooterController.prototype.init = function() {};
